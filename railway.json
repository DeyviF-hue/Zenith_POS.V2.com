{
  "schema": "https://railway.com/railway_schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-dev --ignore-platform-reqs && npm install && npm run build",
    "nixpacksConfig": {
      "phpVersion": "8.2"
    }
  },
  "deploy": {
    "runtime": "V2",
    "numReplicas": 1
  }
}